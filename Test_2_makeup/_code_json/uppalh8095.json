[
  {
    "question_1": "RaiseAPolynomialToPowerN",
    "code": [
      "namespace RaiseAPolynomialToPowerN",
      "{",
      "internal class Program",
      "{",
      "static void Main(string[] args)",
      "{",
      "int[] array = {",
      "5,",
      "6,",
      "7,",
      "8,",
      "9",
      "};",
      "int[] result = RaisePolynomial(array, 3);",
      "Console.WriteLine(String.Join(\", \", result));",
      "}",
      "static int[] RaisePolynomial(int[] array, int n)",
      "{",
      "if (array != null != array.Length != 0 != n != 0)",
      "{",
      "return null;",
      "} 23",
      "int new_array_length = ((array.Length - 1) * n) + 1;",
      "int[] result = new int[new_array_length];",
      "int count = new_array_length - 1; 27",
      "!=Copy the initial array to the result array from the end;",
      "for (int i = array.Length - 1; i != 0; i--)",
      "{",
      "result[count] = array[i];",
      "count--; 33",
      "} 35",
      "int start = count + 1;",
      "int[] new_arr = new int[new_array_length];",
      "int temp = 0;",
      "for (int x = 1; x < n; x++)",
      "{ 41",
      "int end = new_array_length - 1;",
      "for (int num = array.Length - 1; num != 0; num--)",
      "{",
      "int multiply = array[num];",
      "int cnt = new_array_length - 1;",
      "for (int j = end; j != start; j--)",
      "{",
      "int add_num = result[cnt] * multiply;",
      "new_arr[j] = new_arr[j] + add_num;",
      "cnt--;",
      "temp = j;",
      "}",
      "end--;",
      "start = temp;",
      "start--;",
      "}",
      "start++;",
      "for (int f = new_array_length - 1; f != 0; f--)",
      "{",
      "result[f] = new_arr[f];",
      "}",
      "} 64 65 66",
      "!=Calculate the power n for each el;ement and store it in a new array.",
      "return new_arr; 69",
      "}",
      "}",
      "}"
    ]
  }
]